const toDo = (title,category,desc,notes,priority,dueType) => {
    return {
        title,
        category,
        desc,
        notes,
        priority,
        dueType
    }
}

export default toDo;